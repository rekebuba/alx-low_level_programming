#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * str_concat - concatenates two strings.
 * @s1: character
 * @s2: character
 * Return: 0
 */

void free_grid(int **grid, int height)
{
    free(grid);
}