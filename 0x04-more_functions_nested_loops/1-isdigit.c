#include "main.h"

/**
* _isdigit - tests function that prints if integer is positive or negative
* Return: 0
* @c: is uppercase
*/
int _isdigit(int c)
{
if (c >= 65 && c <= 122)
{
return (0);
}
else
{
return (1);
}
}
