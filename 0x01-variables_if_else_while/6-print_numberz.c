#include <stdio.h>
/**
 * main - it prints all single digit numbers of base 10.
 *
 * Return: Always 0
 */
int main(void)
{
int i = 0;
int newline = '\n';
while (i < '\t')
{
putchar(i);
i++;
}
putchar(newline);
return (0);
}
