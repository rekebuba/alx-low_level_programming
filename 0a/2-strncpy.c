#include "main.h"
#include <string.h>

/**
* _strncpy - copies a string.
* Return: 0
* @dest: is pointer
* @src: is pointer
* @n: integer
*/

char *_strncpy(char *dest, char *src, int n)
{
return (strncpy(dest, src, n));
}
