#include "main.h"

/**
 * _isupper - tests function that prints if integer is positive or negative
 * Return: 0
 * @c: is uppercase
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
